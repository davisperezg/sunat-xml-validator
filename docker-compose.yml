version: '3.8'

services:
  sunat-validator:
    restart: always
    build: .
    ports:
      - "8088:8088"
    environment:
      - SERVER_PORT=8088
    volumes:
      - ./logs:/app/logs
    tty: true
    stdin_open: true
